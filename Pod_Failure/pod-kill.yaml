kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: chaos-kill
  namespace: pod-kill
spec:
  selector:
    namespaces:
      - pod-kill
    labelSelectors:
      app: nginx
  mode: one
  action: pod-kill
  duration: 30s
  gracePeriod: 0
