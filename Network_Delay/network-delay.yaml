apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-delay
spec:
  action: partition
  mode: all
  selector:
    namespaces:
      - nginx
    labelSelectors:
      "app": "nginx"
  direction: to
  target:
    selector:
      namespaces:
        - network-chaos
      labelSelectors:
        "app": "httpd"
    mode: all
  duration: "80s"
  delay:
    latency: 25ms
  duration: "60s"